# Folio — Backlog

> **Активный беклог — GitHub Issues:** https://github.com/GarroV/english-bot/issues (фильтр по метке [`folio`](https://github.com/GarroV/english-bot/issues?q=is%3Aissue+is%3Aopen+label%3Afolio)). Находки аудита 2026-06-30 заведены как issues #3–#22 (хвосты MVP M2/M3/M5/M7c/M8, hardening, процесс). Новые задачи — заводить там, не здесь. Этот файл оставлен как заметки/контекст.

> Все идеи, отложенные фичи, пожелания. Сюда пишем всё что не в MVP.
> Не удалять — только помечать статусом.

---

## MVP — отложенные хвосты (по вехам)

> Состояние на 2026-06-17. Ядро M1–M9 закрыто и задеплоено (Cloudflare Workers). Ниже — что осталось внутри вех.

- [ ] **M7 обратное чтение** — бот показывает / повторно отдаёт шаблоны из библиотеки Folio (web→bot), для полного «единого целого». Запись бот→веб сделана 2026-06-18 (`source='bot'`).
- [ ] **M7c доставка домашек** — отправка назначенной домашки ученику через Telegram-бот / email. Блокирует: ученик не нажал Start у бота + нет почтовой инфры/n8n
- [ ] **M8 кабинет ученика** — расписание / баланс / список домашек глазами ученика. Блокирует: онбординг ученика (M7c / отдельный вход ученика)
- [ ] **M2 email magic-link** — вход по email как альтернатива Telegram
- [ ] **M2 инвайт УЧЕНИКА** в существующий workspace (через `folio_invite_tokens`) — отдельно от self-serve регистрации репетитора (M2b сделан)
- [ ] **M3 PII-скраб** при архивации ученика (сейчас только soft-delete через `archived_at`)
- [ ] **M4** — месяц/день-вью календаря; per-user таймзона (сейчас Moscow-only)
- [ ] **M5** — UI переопределения ставки в конкретном занятии (`rate_override` в БД есть); фильтры по периоду; редактирование суммы charge; валюта
- [ ] **M6** — шкала/статистика прогресса (сейчас свободный текст); отдельные записи журнала на ученика для групп; связь журнал ↔ домашка
- [ ] **M9 управление репетиторами** — ручное создание/блокировка/удаление репетиторов и воркспейсов из админки (деструктив; сейчас только выдача инвайтов + read-only список)

## UX / Продукт

- [~] **Дизайн-проход по всей админке** — БАЗА ГОТОВА (2026-06-12): token-тема (тёплые нейтрали + teal + coral), шрифты Oswald (заголовки) + Roboto (текст, кириллица), общий `(app)` layout с сайдбаром, мягкие карточки. ОСТАЛОСЬ: визуальная полировка — «не хватает жизни» (атмосфера/глубина/айдентика). Юзер подберёт референсы. См. `web/design-quality.md`.
- [ ] Интерактивный просмотр домашек в веб-кабинете ученика (без PDF)
- [ ] Push-уведомления в браузере
- [ ] Тёмная тема для кабинета ученика
- [ ] Геймифицированный onboarding для ученика (первый вход)
- [ ] Leaderboard между учениками одного репетитора (опционально)
- [ ] Календарь с синхронизацией в Google Calendar / Apple Calendar
- [ ] Родительский доступ (просмотр прогресса ребёнка)
- [ ] Чат между репетитором и учеником внутри платформы

## Автоматизация

- [ ] **Уведомления живого документа ДЗ (Ф4)** — отложено владельцем 2026-07-03. Реалистична только сторона репетитора: пинг в Telegram «ученик сдал/пересдал задание» (у репетитора всегда есть TG-связка через логин-бота; у ученика канала обычно нет — кабинет по токену). Отправка из веба — через Edge-функцию (`TELEGRAM_BOT_TOKEN` — project-wide Supabase secret, в веб-воркер не тащить): новый `folio-notify` (шаблон `folio-generate` + shared secret) ЛИБО action в `folio-generate`. Триггер — в `markSubmitted` (best-effort). Спека: `docs/superpowers/specs/2026-07-02-homework-live-doc-design.md` §8 Ф4.
- [ ] Автоматическое напоминание о домашке за N дней до дедлайна
- [ ] Автоматический отчёт ученику раз в неделю (прогресс за неделю)
- [ ] Автоматический отчёт репетитору (долги, несданные домашки)
- [ ] Умные уведомления: "ученик не заходил 7 дней"

## Платформа

- [ ] Онбординг-флоу для самостоятельной регистрации репетитора
- [ ] Публичный лендинг Folio
- [ ] Монетизация: subscription для репетиторов
- [ ] Stripe / ЮKassa интеграция
- [ ] PWA / мобильное приложение

## AI / Генерация

- [ ] Генерация плана обучения по уровню и цели ученика
- [ ] Автоматическая оценка сложности домашки
- [ ] Рекомендации следующей темы на основе истории
- [ ] Персонализированные ачивки с картинкой (DALL-E / SVG генерация)

## Аналитика

- [ ] Dashboard статистики для репетитора
- [ ] Сравнение учеников по прогрессу (анонимно)
- [ ] Экспорт данных в CSV / PDF отчёт

## Технический долг (заложить базу сейчас)

- [ ] GDPR-документы: политика конфиденциальности, согласие на обработку данных
- [ ] Timezone: полная поддержка (сейчас Moscow-only)
- [ ] Audit log: кто и что менял в системе
- [ ] Rate limiting на Edge Functions
- [ ] Rate limiting на auth-роутах `/api/auth/telegram/{start,status}` — сейчас без троттлинга (создание токенов / поллинг). Не критично пока одно-арендный bootstrap, но обязательно до публичного запуска (security-review M2a, 2026-06-12)
- [ ] Backup стратегия для Supabase

### Hardening из ревью (2026-06-15..17)

- [ ] **`folio_invite_tokens`: добавить `WITH CHECK` + revoke authenticated-записи** при реализации инвайтов ученика — сейчас политика USING-only (как был баг у `folio_users`), но таблица не используется. См. [[project_folio_rls_invariants]] (memory). (security-review M9)
- [ ] **Минимизация PII в RSC-payload** — `StudentsPanel` прокидывает полный `StudentRow` (email/telegram/notes) в клиент, хотя показывает только имя+ставку (нужно для edit-диалога). Передавать полный профиль лениво. (review M3.1)
- [ ] **Типизация next-intl messages** (AppConfig/Messages augmentation) — опечатка ключа сейчас падает в рантайме, а не на компиляции
- [ ] **`server-only` pin** на `lib/supabase/admin.ts` — defense-in-depth, чтобы service-role клиент нельзя было случайно затащить в клиентский бандл
- [ ] **Поллинг логина 180s vs TTL токена 300s** — рассинхрон: подтверждение в окне 3–5 мин теряется (login UX, pre-existing)
- [ ] **Хардкод `locale:"ru"`** в auth-редиректах страниц `(app)` — EN-юзера кидает на `/ru/login` (i18n, pre-existing)
- [ ] **Активировать webhook secret бота** — `index.ts` сверяет `X-Telegram-Bot-Api-Secret-Token` с `TELEGRAM_WEBHOOK_SECRET`, но пока fail-open (секрет не задан). Задать секрет + перерегистрировать webhook с `secret_token`; иначе `from.id` спуфится → межтенантная запись через мост бот→веб. (security-review M7, 2026-06-18, **CRITICAL** — было задеплоено живым) [[project_folio_rls_invariants]]
- [ ] **Defense-in-depth для bot-bridge** (опц.; ревью M7 отклонило как неэксплуатируемое при текущих ограничениях): композитная проверка `created_by`↔`workspace_id` на уровне БД; сверка `provider_uid`↔`folio_users.telegram_id` в `resolveFolioWorkspace`.
- [ ] **Поллинг чата ДЗ — только активный тред** (security-review Ф3, 2026-07-03, LOW) — сейчас каждый смонтированный `ChatThread` поллит раз в 10с; в кабинете ученика N карточек ДЗ = N параллельных поллеров (service-role запросы). Пауза в фоновой вкладке уже есть (Page Visibility). Глубже: поллить только раскрытый/видимый тред (свернуть чат в карточке под кнопку) — когда упрётся в стоимость. Rate-limit на постинг сообщений — отдельно, инфра CF WAF (#10).
- [ ] **Интеграционные тесты server-actions живого документа ДЗ** (security-review Ф2, 2026-07-03) — сейчас юнит-тесты покрывают только чистую логику (`derive.ts`, `assignments-schema.ts`). Добавить смоук против локального Supabase на кросс-тенант/кросс-ученик и гейты стейт-машины: `commentOnItem`/`returnAssignment`/`acceptAssignment`/`loadReview` (репетитор чужого воркспейса → `not found`), `saveAnswer`/`markSubmitted` (чужой токен/чужой assignmentId → `not found`; заморозка правки в `submitted`/`accepted`). Это ключевое security-свойство фичи — юнит-тесты на `derive.ts` его не ловят.

### Hosting / деплой

- [ ] **Workers Builds** — авто-деплой из GitHub на каждый push (сейчас ручной `npm run cf:deploy`), root=`folio/`; отдельный **scoped CF-токен** для CI
- [ ] **Ротировать dev CF-токен** — текущий токен светился в чате (юзер решил пока не ротировать); сделать до публичного запуска
- [ ] **Кастомный домен** вместо `folio.vasiliy-garro.workers.dev`
