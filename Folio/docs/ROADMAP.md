# Folio — Roadmap

> Последнее обновление: 2026-06-08

---

## MVP — Запуск для первого репетитора

### M1: Фундамент
- [x] Инициализация Next.js проекта (App Router, TypeScript, Tailwind, shadcn/ui) — `folio/`
- [x] ~~Supabase проект (отдельный от Swarm/english-bot)~~ Решено: используем существующий проект `btlglelwxazdxfqdmcti` (общий с english-bot), таблицы Folio с префиксом `folio_` (см. [[002-multitenancy]])
- [x] Базовые миграции: folio_workspaces, folio_users, folio_auth_methods, folio_invite_tokens (`20260608120000_folio_init.sql`)
- [x] RLS политики на все таблицы (через `folio_current_workspace_id()`)
- [x] i18n setup (next-intl, ru/en)
- [x] CLAUDE.md, DATA_MODEL.md, ARCHITECTURE.md в репо

### M2: Auth
> M2a (2026-06-12): Telegram-login ядро + seed super_admin + защита роутов. Email/инвайты/Login Widget — позже.
> M2b (2026-06-16): self-serve регистрация РЕПЕТИТОРА по инвайт-ссылке (Telegram) → свой workspace + демо-данные. Атомарный provisioning (RPC `folio_register_tutor`), проверено e2e на проде. Выдача инвайтов — пока скриптом (UI супер-админа в M9).
- [x] Self-serve регистрация репетитора по инвайту (`folio_signup_invites` → новый workspace, демо-данные)
- [ ] Email magic link (Supabase Auth) (отложено в M2a)
- [ ] Инвайт-флоу УЧЕНИКА: репетитор создаёт ученика → письмо → активация (отдельно от M2b)
- [x] Super Admin создан (seed-bootstrap, telegram_id 744230399 — временно)
- [x] Telegram login (bot deep-link + одноразовый токен; не Login Widget)
- [x] Роли и middleware (защита роутов через `middleware.ts` (Edge) + `getUser()`)

### M3: Students
> M3 (2026-06-12): CRUD учеников + мягкая архивация. Логин ученика / скраб PII — позже.
> M3.1 (2026-06-16): экран учеников слит с расписанием — ученики стали правой колонкой на `/schedule` (`StudentsPanel`); `/students` редиректит на `/schedule`, `StudentsTable` удалён.
- [x] Список учеников в админке репетитора
- [x] Создание/редактирование профиля ученика
- [x] Дефолтная ставка за урок
- [x] Архивация (soft-delete; PII-скраб отложен — в M3 только мягкая архивация через `archived_at`)

### M4: Schedule
> M4 (2026-06-13): недельный календарь-сетка + занятия (solo/группа), drag-and-drop переноса, быстрое «состоялось» кнопкой ✓ на карточке. Месяц/день-вью, per-user TZ — позже. Триггер биллинга при «состоялось» — реализован в M5; журнал — M6.
> M4.1 (2026-06-16): сетка компактнее по высоте (`HOUR_PX` 76→48), карточки занятий одной строкой; экран объединён с учениками в двухпанельный (расписание + правая колонка учеников).
- [x] Интерактивная доска (calendar view)
- [x] Создание занятия (solo / group, online / offline)
- [x] Перенос занятия (изменение даты)
- [x] Отмена занятия
- [x] Отметка "состоялось" → триггер журнала
- [x] Провал в занятие: карточка с данными ученика

### M5: Billing Tracker
> M5a (2026-06-15): леджер `folio_student_payments` (charge/payment), автоначисление при «состоялось» (реверс при возврате/отмене), фиксация оплат, баланс/долги по ученикам. Per-lesson override UI и фильтры по периоду — позже.
- [x] Автоначисление при отметке "состоялось" (реверс при reopen/cancel; сумма = rate_override ?? default_rate)
- [ ] Переопределение ставки в конкретном занятии (колонка `rate_override` есть, UI — позже)
- [x] Фиксация оплаты репетитором вручную
- [x] Баланс ученика (charged − paid, агрегируется из леджера)
- [x] Таблица долгов в админке (долги подсвечены; фильтры по периоду — позже)

### M6: Lesson Journal
> M6 (2026-06-16): запись на занятие (`folio_lesson_journal`, одна на занятие). ✓ «состоялось» остаётся быстрым; журнал — кнопкой «Журнал занятия» на состоявшемся занятии. История по ученику — кликом по имени в правой колонке. Прогресс — свободный текст (шкала/статистика отложены).
- [x] Форма журнала (кнопкой на состоявшемся занятии; ✓ не прерывается)
- [x] История записей по ученику (через ростер: journal → lesson → lesson_students)
- [x] Поля: тема, уровень (CEFR), комментарий, прогресс (свободный текст)

### M7: Homeworks (Bot Bridge + генерация)
> Решено 2026-06-13: веб и бот РАВНОСИЛЬНЫ. Генерация заданий должна быть и в веб-Folio, не только в боте — общий движок генерации (те же промпты Claude), оба пишут в `homework_templates`. (Обновляет MASTER §8.)
> M7a (2026-06-13): общий движок (_shared/generate.ts) + folio-generate Edge Function + окно генерации в вебе с сохранением шаблона.
> M7b (2026-06-15): назначение шаблона ученику(ам) + дедлайн + статус, просмотр/копирование контента шаблона. Авто-доставка (TG/email) отложена в M7c — бот не может писать ученику, не нажавшему Start; почты/n8n нет.
- [x] Общий движок генерации (shared core), вызывается из веба и из бота
- [x] Форма генерации задания в веб-Folio (тема/уровень/тип → сгенерировать → сохранить шаблон)
- [x] Список домашек/шаблонов в админке репетитора
- [x] Назначение домашки ученику (`folio_homework_assignments` + дедлайн + статус)
- [x] english-bot пишет в `folio_homework_templates` (мост бот→веб, 2026-06-18; запись при скачивании PDF, `source='bot'`; webhook аутентифицируется через `TELEGRAM_WEBHOOK_SECRET`)
- [ ] Отправка через Telegram бота / email (M7c — после онбординга ученика в бот / почты)

### M8: Student Cabinet
- [ ] Расписание ученика (solo/group пометки)
- [ ] Баланс и история оплат
- [ ] Список домашек и их статус

### M9: Super Admin
> M9 core (2026-06-16): админка `/admin` (только super_admin) — выпуск/копирование/отзыв инвайтов репетиторов + read-only список воркспейсов (репетитор, число учеников/занятий). Гейт `getSuperAdmin()` в странице И в каждом server-action. Ручное создание/блокировка репетиторов/воркспейсов — позже.
- [x] Список всех воркспейсов (read-only: репетитор, ученики, занятия)
- [x] Выдача инвайтов репетиторов из UI (создать/копировать/отозвать)
- [x] Создание воркспейса (через self-serve инвайт — M2b; репетитор регистрируется сам)
- [ ] Ручное создание/блокировка репетитора (деструктив — позже)

---

## V2 — После обратной связи

- [ ] Progress + Achievements (ачивки через OpenAI)
- [ ] Template Editor (редактор промптов в UI)
- [ ] Notifications: UI настройки, n8n триггеры
- [ ] Statistics dashboard для репетитора
- [ ] Групповые занятия: полная поддержка состава
- [ ] Google Calendar sync

---

## V3 — Масштабирование

- [ ] Онбординг второго репетитора (самостоятельная регистрация)
- [ ] Публичный лендинг Folio
- [ ] Монетизация: subscription
- [ ] PWA / мобильное приложение
- [ ] GDPR документы и согласия
